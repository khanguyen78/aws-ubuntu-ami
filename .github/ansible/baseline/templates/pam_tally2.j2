# {{ ansible_managed }}
Name: tally2 lockout after failed attempts enforcement
Default: yes
Priority: 1024
Conflicts: cracklib
Auth-Type: Primary
Auth-Initial:
  required      pam_tally2.so deny={{ baseline_pam_tally2_auth_retries }} onerr=fail unlock_time={{ baseline_pam_tally2_auth_lockout_time }}
Account-Type: Primary
Account-Initial:
  required      pam_tally2.so
